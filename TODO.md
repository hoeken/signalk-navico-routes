# 1.0.0

- admin UI cleanup
- gofree discovery
- increase mfd sync timeout to 120s
- default sync speed to 15 mins
- Confirm the true name-length limit on hardware (codec currently truncates at 32 chars; MFD files contain up to 24).

- screenshots